// SettingsView.swift
// Placeholder for settings — built later.

import SwiftUI

struct SettingsView: View {
    var body: some View {
        Text("Settings — coming later")
            .font(.title3)
            .foregroundStyle(.secondary)
    }
}

#Preview {
    SettingsView()
}
