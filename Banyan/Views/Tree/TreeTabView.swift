// TreeTabView.swift
// Placeholder for the tree canvas — built in step 3.

import SwiftUI

struct TreeTabView: View {
    let ownerPersonId: UUID

    var body: some View {
        Text("Tree — coming in step 3")
            .font(.title3)
            .foregroundStyle(.secondary)
    }
}

#Preview {
    TreeTabView(ownerPersonId: UUID())
}
