// ContentView.swift
// Placeholder root view — replaced in step 2.

import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("Banyan")
            .font(.largeTitle)
    }
}

#Preview {
    ContentView()
}
